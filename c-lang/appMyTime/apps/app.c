#include <stdio.h>
#include "../includes/float_vector.h"


int main ()
{
    // FVector *vec = FVector_create(2); 
    // FVector_print(vec);

    // FVector_append(vec, 0.0);
    // FVector_append(vec, 1.0);
    // FVector_print(vec);

    // FVector_destroy(&vec);
    printf("Main running...");

    return 0;
}